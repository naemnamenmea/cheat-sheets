Проверка проекта:

    * git remote -v
    * git pull
    * exclude & .gitignore
    * git stash list
    * empty folders
    * git status -> commits & staged & unstaged changes

    git add .
    git commit -S -m "refactoring"
    git push